/** @hidden */ /** */
/** Converts size string to corresponding size in logical pixels */
declare function toPixels(size: string): number | null;
/** clamp number between min and max values */
declare const clamp: (number: number, min: number, max: number) => number;
export { toPixels, clamp };
